---

# have the FS fill the SD card
config_expand_filesystem: true
# user to create
config_user:
  uid:
  guid:
  name:
  password:
  path_to_ssh_keys: # https://github.com/username.keys or directory
# default pi user
default_config_user:
  name: pi
  password: raspberry
config_influxdb:
  host:
  port:
config_influxdb_collectd:
  host: 0.0.0.0
  db: collectd
  writers:
  - user:
    pass:
  reader:
    user:
    pass:
config_influxdb_pfsense:
  host: 0.0.0.0
  db: pfsense
  writer:
    user:
    pass:
  reader:
    user:
    pass:
config_ssh_port: 22
config_reboot_max_wait_time: 300
config_reboot_min_wait_time: 5
config_transmission_user:
config_transmission_pass:
config_plex_gid:
config_nfs_mount_cmd:
